

test('xx', () => {
    expect(1).toBe(1);
});

/*
everything is an expression:
- the name of a binding ("word")
- a number, ("value")
- a string, ("value")
- an application ("apply")
- identifiers ("word")
 */